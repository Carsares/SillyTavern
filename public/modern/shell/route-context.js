export function createRouteContext({
    state,
    activity,
    api,
    assets,
    characters,
    chatContext,
    chatFiles,
    chatGeneration,
    chatMessages,
    components,
    constants,
    extensions,
    groups,
    personas,
    presets,
    settings,
    shell,
    utils,
    worldbooks,
}) {
    return {
        state,
        ...utils,
        ...components,
        ...shell,
        ...activity,
        ...constants,
        ...api,
        ...assets,
        ...characters,
        ...chatContext,
        ...chatFiles,
        ...chatGeneration,
        ...chatMessages,
        ...extensions,
        ...groups,
        ...personas,
        ...presets,
        ...settings,
        ...worldbooks,
    };
}
